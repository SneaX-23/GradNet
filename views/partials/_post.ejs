<article class="post"> 
    <div class="post-header"> 
        <div class="post-avatar"><%= event.author_name.charAt(0).toUpperCase() %></div>
        <div class="post-info"> 
            <div class="post-author"> 
                <span class="author-name"><%= event.author_name %></span> 
                <span class="author-handle">@handle</span> 
                <span class="post-time">• <%= new Date(event.created_at).toLocaleString() %></span> 
            </div>
        </div>
    </div>
    <div class="post-content"> 
        <p><%= event.description %></p> 
        <% if (event.image_url) { %>
            <div class="post-image"> 
                <img src="<%= event.image_url %>" alt="Event Image" style="width:100%; border-radius: 8px;">
            </div>
        <% } %> 
    </div>
    <div class="post-actions"> 
        <button class="action-btn"> 
            <span class="action-icon">💬</span><span class="action-count">0</span> 
        </button>
        <button class="action-btn"> 
            <span class="action-icon">🔄</span><span class="action-count">0</span>
        </button>
        <button class="action-btn"> 
            <span class="action-icon">❤️</span><span class="action-count">0</span>
        </button>
        <button class="action-btn"> 
            <span class="action-icon">📤</span> 
        </button>
    </div>
</article>