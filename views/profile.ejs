<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your Profile - GradNet</title>
    <link rel="stylesheet" href="/css/base.css">
    <link rel="stylesheet" href="/css/home.css"> 
    <link rel="stylesheet" href="/css/profile.css"> 
</head>
<body>
    <div class="main-container">

        <%- include('partials/_sidebar', { user: user }) %>

        <main class="main-content">
            <div class="content-wrapper">
                <header class="content-header">
                    <h1>Profile</h1>
                </header>

                <div class="profile-page">
                    <div class="profile-header card">
                        <div class="profile-picture">
                            <img src="<%= user.profile_picture_url  %>" alt="Profile Picture">
                        </div>
                        <div class="profile-summary">
                            <h2 class="profile-name"><%= user.name %></h2>
                            <p class="profile-department"><%= user.department %></p>
                        </div>
                        <button id="editProfileBtn" class="btn btn-secondary btn-small">Edit Profile</button>
                    </div>

                    <div class="profile-body">
                         <div class="profile-about card">
                            <h3>About</h3>
                            <p><%= user.bio || 'No bio available. Click Edit Profile to add one!' %></p>
                        </div>
                        <div class="profile-details card">
                            <h3>Details</h3>
                            <ul>
                                <li><strong>USN:</strong> <span><%= user.usn %></span></li>
                                <li><strong>Role:</strong> <span><%= user.role %></span></li>
                                <li><strong>Email:</strong> <span><%= user.email %></span></li>
                                <li><strong>Phone:</strong> <span><%= user.phone || 'Not provided' %></span></li>
                                <li><strong>Graduation Year:</strong> <span><%= user.graduation_year || 'N/A' %></span></li>
                                <% if (user.linkedin_url) { %>
                                    <li><strong>LinkedIn:</strong> <a href="<%= user.linkedin_url %>" target="_blank">View Profile</a></li>
                                <% } %>
                            </ul>
                        </div>
                    </div>
                </div>
            </div>
        </main>
    </div>

    <div id="editProfileModal" class="modal-overlay">
        <div class="modal-content">
            <div class="modal-header">
                <h2>Edit profile</h2>
                <button id="closeModalBtn" class="close-button">&times;</button>
            </div>
            <form action="/home/profile/update" method="POST" enctype="multipart/form-data">
                
                <div class="profile-image-editor">
                    <div class="header-image-preview" id="headerPreview">
                        <label for="headerImageInput" class="image-upload-label" title="Add header image">📷</label>
                        <input type="file" id="headerImageInput" name="headerImage" class="visually-hidden" accept="image/*">
                    </div>
                    <div class="profile-picture-preview" id="profilePicPreview">
                         <img src="<%= user.profile_picture_url  %>" alt="Profile Picture">
                         <label for="profileImageInput" class="image-upload-label" title="Add profile image">📷</label>
                         <input type="file" id="profileImageInput" name="profileImage" class="visually-hidden" accept="image/*">
                    </div>
                </div>
                <div class="form-group">
                    <label for="name" class="form-label">Name</label>
                    <input type="text" id="name" name="name" class="form-input" value="<%= user.name %>">
                </div>
                <div class="form-group">
                    <label for="bio" class="form-label">Bio</label>
                    <textarea id="bio" name="bio" class="form-input" rows="3"><%= user.bio %></textarea>
                </div>
                <!-- <div class="form-group">
                    <label for="department" class="form-label">Department</label>
                    <input type="text" id="department" name="department" class="form-input" value="<%= user.department %>">
                </div> -->
                <div class="form-group">
                    <label for="phone" class="form-label">Phone</label>
                    <input type="tel" id="phone" name="phone" class="form-input" value="<%= user.phone %>">
                </div>
                 <div class="form-group">
                    <label for="linkedin_url" class="form-label">LinkedIn URL</label>
                    <input type="url" id="linkedin_url" name="linkedin_url" class="form-input" value="<%= user.linkedin_url %>">
                </div>
                <button type="submit" class="btn btn-primary">Save</button>
            </form>
        </div>
    </div>

    <script src="/js/profile.js"></script>
</body>
</html>